apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: config-server
  name: config-server
spec:
  ports:
    - name: http
      port: 8088
      protocol: TCP
      targetPort: 8088
      nodePort: 30090
  selector:
    app: config-server
  type: NodePort
status:
  loadBalancer: {}
